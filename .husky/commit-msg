#!/usr/bin/env sh
# Enforce Conventional Commits via commitlint. The message file path is
# passed in $1 by git. `--edit` tells commitlint to read from that path
# rather than stdin.
set -e

pnpm exec commitlint --edit "$1"
